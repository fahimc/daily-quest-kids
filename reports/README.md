# Reports

Content validation, fixture statistics and human-review reports are generated
here by `content-tools`.

- `validation.*`, `puzzles.csv` and `human-review.csv` describe the preview
  pack.
- `season-one-*` files describe the Phase 14 full-season candidate export and
  explicitly list release blockers.
